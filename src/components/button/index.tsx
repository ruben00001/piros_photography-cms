const Button = ({}: { type: "" }) => {
  return (
    <button
      className="btn-outline btn-sm btn rounded-md font-normal capitalize"
      type="button"
    >
      Cancel
    </button>
  );
};

export default Button;
